# Supabase (obrigatório para runtime e scripts)
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Multi-tenant: tenant padrão (Meta-Lav). Usado por lib/tenant quando não há header x-tenant/JWT.
# Staging/prod: mesmo UUID do registro em public.tenants.
DEFAULT_TENANT_ID=b5bec38d-dfec-4f33-821d-4f1c7f44db32

# Opcional: ambiente para scripts (local | ci | prod)
# ENV=local

# Opcional: base URL do app (scripts E2E)
# BASE_URL=http://localhost:3000
